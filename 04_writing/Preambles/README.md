# Preambles

Shared LaTeX preambles used by both `Paper/` and `Slides/`.

## Usage

Referenced via `TEXINPUTS=../Preambles:$TEXINPUTS` during compilation.
Place your `header.tex`, custom commands, and package imports here.
